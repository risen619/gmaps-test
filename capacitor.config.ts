import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'google-maps-api-script-fail',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
